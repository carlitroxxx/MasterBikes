# Nombre de la aplicación
spring.application.name=arriendo-service

# Conexión a MongoDB Atlas (usará variable de entorno en Render)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://duoc:duoc1234@cluster0.ocb6vrn.mongodb.net/mb_arriendo_db?retryWrites=true&w=majority}

# Puerto del servicio (Render asignará uno automáticamente en producción)
server.port=${PORT:8084}

# URL del servicio de inventario (usará variable de entorno en Render)
app.inventario-service.url=${APP_INVENTARIO_SERVICE_URL}

# Configuración de RestTemplate (timeouts)
rest.template.connection-timeout=5000
rest.template.read-timeout=5000